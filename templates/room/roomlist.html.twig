{% block body %}
<h1>Room List</h1>
<table border="5">
    <thead>
        <tr>
            <th>id</th>
            <th>name</th>
        </tr>
    </thead>
    <tbody>
        {% for room in room %}
        <tr>
        
        <td>{{ room.id }}</td>
            <td>{{ room.name }}</td>
            <td>
                <a href ="{{path('room_update',{'id':room.id})}}">Edit</a> |
                <a href ="{{path('room_delete',{'id':room.id})}}">Delete</a>
                
                
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
<a href ="{{path('room_add')}}"><button> Add </button></a>
<a href ="{{path('student_show')}}"><button> Student list </button></a>
{% endblock %}